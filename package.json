{
    "name": "devcontainer",
    "private": true,
    "scripts": {
        "prepare": "husky install .devcontainer/husky",
        "precommit": "ls -al"
    },
    "devDependencies": {
        "husky": "^6.0.0"
    }
}
